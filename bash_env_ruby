# Load RBenv
[[ -s "/usr/bin/env rbenv" ]] && eval "$(command rbenv init -)"

# Aliases
alias rspec="LOGGER_STDOUT=false bundle exec rspec --format documentation"

[[ -d "$HOME/.rbenv/shims" ]] && PATH="$HOME/.rbenv/shims:$PATH"
